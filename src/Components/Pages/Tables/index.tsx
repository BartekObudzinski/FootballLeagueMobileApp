import React from 'react';
import { View } from 'react-native';
import { Table } from '../../Organisms/Table';

export const Tables = () => {
  return (
    <View>
      <Table />
    </View>
  );
};
