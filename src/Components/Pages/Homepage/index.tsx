import { SafeAreaView } from 'react-native-safe-area-context';
import { Typography } from '../../Atoms/Typography/styles';

export const Homepage = () => {
  return (
    <SafeAreaView>
      <Typography>Homepage Screen</Typography>
    </SafeAreaView>
  );
};
